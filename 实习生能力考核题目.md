# AI 编程题：命令行 HTTP 接口压测工具

## 一、任务背景

请设计并实现一个命令行 HTTP 接口压力测试工具，用于向指定 HTTP 地址发起固定数量的并发请求，并输出压测统计结果。

工具需要自行控制并发和资源占用，在请求失败、超时或用户主动中止时能够正确结束。

本题重点考察：

- 并发编程能力
- HTTP 资源管理
- 超时与取消处理
- 数据统计正确性
- 代码结构和测试习惯
- 使用 AI 后的验证与判断能力

## 二、时间和工具

- 建议完成时间：30 分钟
- 编程语言不限
- 可以使用任意第三方库
- 可以使用 ChatGPT、Claude、Cursor、Copilot 等 AI 编程工具
- 不要求完成所有加分项
- 候选人需要能够解释和修改提交的代码

## 三、命令行接口

工具名称不限，以下以 `httpload` 为例。

最低需要支持：

```bash
httpload \
  --url http://localhost:8080/ \
  --requests 1000 \
  --concurrency 20 \
  --timeout 2s
```

参数说明：

| 参数 | 说明 |
|---|---|
| `--url` | 被压测的 HTTP 地址 |
| `--requests` | 总请求数 |
| `--concurrency` | 最大并发请求数 |
| `--timeout` | 单个请求的超时时间 |

允许增加短参数，例如：

```bash
httpload -u http://localhost:8080/ -n 1000 -c 20 -t 2s
```

必做部分只要求支持 HTTP GET。

## 四、功能要求

### 1. 并发控制

工具需要发出 `--requests` 指定数量的请求。

任意时刻正在执行的 HTTP 请求数不得超过 `--concurrency`。

例如：

```bash
httpload --url http://localhost:8080/ \
  --requests 1000 \
  --concurrency 20 \
  --timeout 2s
```

要求：

- 总共计划执行 1000 个请求
- 同时执行的请求最多为 20 个
- 不允许为全部请求一次性创建无界数量的线程、协程或任务
- 请求结束后不能遗留后台执行单元

具体采用 worker pool、信号量或其他并发模型，由候选人自行决定。

### 2. HTTP 请求处理

每个请求需要：

- 使用指定 URL
- 应用单请求超时
- 正确关闭响应体
- 不在内存中长期保存完整响应正文
- 尽可能复用 HTTP Client 和底层连接

状态码分类：

- `200～299`：成功
- 其他状态码：非 2xx
- 网络连接失败：错误
- 超过单请求超时时间：超时

### 3. 参数校验

至少处理：

- URL 缺失
- URL 格式非法
- `requests <= 0`
- `concurrency <= 0`
- timeout 格式非法或小于等于 0

当并发数大于请求数时，可以：

- 返回参数错误；或者
- 将实际并发数降为请求数

但必须明确说明行为。

### 4. 压测结果

压测结束后至少输出：

```text
Target:         http://localhost:8080/
Requests:       1000
Concurrency:    20

Completed:      1000
Succeeded:      970
Non-2xx:        15
Errors:         5
Timeouts:       10

Elapsed:        2.35s
Requests/sec:   425.53
Avg latency:    43.21ms
Min latency:    10.14ms
Max latency:    201.45ms
```

输出格式不需要完全相同，但统计信息必须清晰。

正常完成时应满足：

```text
Completed = Succeeded + Non-2xx + Errors + Timeouts
```

延迟统计以已完成的请求为准。

### 5. 优雅退出

压测过程中，用户可能按下 `Ctrl+C`。

收到中止信号后，工具应当：

1. 停止调度新的请求
2. 通知正在执行的请求结束或取消
3. 不长时间挂起
4. 输出已经完成部分的统计结果
5. 明确显示压测被中止

示例：

```text
Interrupted:    true
Scheduled:      420
Completed:      405
Succeeded:      390
Non-2xx:        5
Errors:         2
Timeouts:       8
```

中止后不要求补齐原计划请求数。

## 五、本地测试接口

为了避免压测公共服务，可以使用本地静态 HTTP Server。

确保安装了 Node.js，然后在任意目录执行：

```bash
npx serve -l 8080
```

启动后，正常接口为：

```text
http://localhost:8080/
```

测试命令：

```bash
httpload \
  --url http://localhost:8080/ \
  --requests 10000 \
  --concurrency 50 \
  --timeout 2s
```

测试非 2xx 统计：

```bash
httpload \
  --url http://localhost:8080/not-found \
  --requests 1000 \
  --concurrency 20 \
  --timeout 2s
```

不存在的路径预计返回 404。

测试网络错误：

```bash
httpload \
  --url http://localhost:65530/ \
  --requests 100 \
  --concurrency 10 \
  --timeout 500ms
```

运行时间较长的压测时，可以按下 `Ctrl+C` 验证优雅退出。

## 六、测试要求

至少提供两个自动化测试，测试方向可以自行选择。

建议覆盖：

- 实际请求总数正确
- 最大并发数不超过配置
- 2xx 与非 2xx 分类正确
- 慢请求触发超时
- 网络错误不会造成死锁
- 统计数量守恒
- 中止后可以正常退出

自动化测试不得依赖公网服务。

可以在测试中启动一个本地 HTTP Server，返回指定状态码或延迟响应。

## 七、提交内容

请提交：

- 可运行的源代码，最好放到 Github 上
- 简短的运行说明
- 自动化测试
- 一条示例压测命令

如果没有完成全部功能，请在说明中列出：

- 已完成的部分
- 未完成的部分
- 如果继续开发，准备如何完成

不要求提供：

- Web 页面
- Docker
- 数据库
- 分布式压测
- 图表系统
- 云端部署
- 生产级监控
